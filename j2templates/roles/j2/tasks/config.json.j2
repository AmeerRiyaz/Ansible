{
  "appName": "{{ app_name }}",
  "env": "{{ app_env }}",
  "port": {{ app_port }},
  "db": {
    "host": "{{ database.host }}",
    "port": {{ database.port }},
    "user": "{{ database.user }}",
    "password": "{{ database.password }}"
  }
}
